global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  # Scrape the OTLP metrics the collector re-exposes in Prometheus format.
  - job_name: otel-collector
    static_configs:
      - targets: ["otel-collector:8889"]
